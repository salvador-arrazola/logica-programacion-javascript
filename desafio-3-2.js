// 2. Crea un contador que comience en 10 y vaya hasta 0 usando un bucle 'while'.
//    Muestra cada número.

// Inicializamos el contador en 10.
let contador = 10;

// Mientras que el contador sea mayor o igual a 0.
while (contador >= 0) {
    // Desplegamos el valor del contador.
    alert(`El contador es igual a ${contador--}.`);
}
