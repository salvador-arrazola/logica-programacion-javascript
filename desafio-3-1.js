// 1. Crea un contador que comience en 1 y vaya hasta 10 usando un bucle 'while'.
// Muestra cada número.

// Inicializamos el contador en 1.
let contador = 1;

// Mientras que el contador sea menor o igual a 10.
while (contador <= 10) {
    // Desplegamos el valor del contador.
    alert(`El contador es igual a ${contador++}.`);
}
